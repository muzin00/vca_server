[project]
name = "vca-worker"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "celery[redis]>=5.6.2",
    "faster-whisper>=1.2.1",
    "wespeakerruntime>=1.0.1",
    "pydantic-settings>=2.0.0",
    "torch==2.4.1",
    "torchaudio==2.4.1",
    "soundfile==0.13.1",
    "vca-core",
    "vca-infra",
]

[tool.uv.sources]
vca-core = { workspace = true }
vca-infra = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["vca_worker"]
