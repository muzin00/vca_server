[project]
name = "vca-core"
version = "0.1.0"
requires-python = ">=3.10,<4.0"
dependencies = ["sqlmodel", "pydantic", "python-ulid>=3.1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["vca_core"]
